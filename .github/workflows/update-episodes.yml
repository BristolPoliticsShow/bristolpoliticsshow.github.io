name: Update Content

on:
  schedule:
    # Once a day at 06:00 UTC. The show is weekly, so daily is plenty.
    - cron: "0 6 * * *"
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  update-content:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: pip install beautifulsoup4

    - name: Build content JSON from the RSS feed
      run: python3 scripts/build_content.py

    - name: Commit and push if changed
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add audio.json thisweek.json stories.json
        git diff --staged --quiet || git commit -m "Update show content"
        git push
